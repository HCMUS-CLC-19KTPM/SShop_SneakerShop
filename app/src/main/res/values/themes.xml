<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.SShop_SneakerShop" parent="Theme.Material3.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>
    <!-- 1. -->
    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <!-- 2. -->
        <item name="windowSplashScreenBackground">@color/primary</item>

        <!-- 3. -->
        <item name="windowSplashScreenAnimatedIcon">@drawable/logo_size</item>
        <item name="windowSplashScreenAnimationDuration">2000</item>
        <!-- 4. -->
        <item name="android:windowSplashScreenIconBackgroundColor">
            @color/white
        </item>

        <!-- 5. -->
        <!-- 6. -->
        <item name="postSplashScreenTheme">@style/Theme.SShop_SneakerShop</item>
    </style>

    <style name="Theme.SShop_SneakerShop.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.SShop_SneakerShop.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.SShop_SneakerShop.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>